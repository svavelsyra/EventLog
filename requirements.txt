# EventLog - Application Requirements
# Python 3.14

# Application Dependencies
sqlcipher3>=0.6.2  # SQLCipher-backed SQLite binding for encrypted database support

# Standard library modules still used directly by the application:
# - tkinter (GUI)
# - sqlite3 (existing plaintext adapter/tests and compatibility paths)
# - hashlib.pbkdf2_hmac (shared PBKDF2 helper; no extra KDF dependency currently needed)

# For development/testing dependencies, see requirements-test.txt


