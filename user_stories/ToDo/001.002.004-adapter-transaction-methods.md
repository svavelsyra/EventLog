# User Story: 001.002.004 - Transaction Contract & Delegation Semantics

**Story Number**: 001.002.004  
**Created**: 2026-04-23  
**Status**: ToDo  
**Epic**: [001 - Database Foundation](001.000.000-database-foundation.md)  
**Parent Story**: Split from 001.002 - Database Adapter and Repository Contract Foundation  
**Owner**: Developer

---

## Purpose (WHY) ⭐ MOST IMPORTANT

**Why does this story exist?**

Transaction management is essential for data integrity in multi-step operations. Under the approved architecture, transaction behavior must be clear at both levels:

1. **Low-level atomic operations** - The adapter must expose the primitives that make multi-step changes succeed or fail together
2. **Repository-facing delegation** - Repositories need clear transaction semantics without re-owning low-level DB mechanics
3. **Test isolation** - Tests can rollback changes for clean state
4. **Error recovery** - Failed operations can be rolled back without partial data corruption

Example use cases:
- Personnel status update (create new entry + mark old inactive) - both must succeed
- Bulk operations - all entries or none
- Test setup - rollback after each test

Without transaction control, the application cannot guarantee data integrity for complex operations.

### User Value
- Data integrity guaranteed for multi-step operations
- Tests can run in isolation (rollback after each test)
- Failed operations don't leave partial data
- Clean shutdown (connections closed properly)

---

## Goal (WHAT)

**What functionality are we delivering?**

Complete the transaction contract direction for the approved adapter/repository split by defining low-level adapter transaction methods and the repository-facing delegation expectations that concrete repositories will follow.

### Acceptance Criteria

- [ ] `DatabaseAdapter` transaction capabilities are defined:
  - Can start explicit transaction for multi-step atomic operations
  - Can commit transaction (make changes permanent)
  - Can rollback transaction (discard changes for error recovery/test cleanup)
- [ ] Repository-facing transaction methods are documented as delegation points to the adapter layer
- [ ] Transaction naming is consistent across the approved architecture (for example `begin_transaction()`, `commit_transaction()`, `rollback_transaction()`)
- [ ] Connection cleanup expectations remain clear across the split (`close()` still releases resources cleanly)
- [ ] All methods use `@abstractmethod` decorator
- [ ] Return type annotations present (even for None)
- [ ] Docstrings explain when and why to use each capability
- [ ] Method grouping separates transaction semantics from non-transaction responsibilities

---

## Limitations (CONSTRAINTS & REQUIREMENTS)

### Hard Constraints (MUST)
- **Respect approved split** - Low-level transaction primitives belong to `DatabaseAdapter`; repository-facing transaction methods delegate downward
- **Type hints required** - Even for None returns
- **Abstract methods** - Use @abstractmethod decorator (no implementation)
- **Keep naming consistent** - Transaction method names should align with the approved architecture docs

### Negative Constraints (MUST NOT)
- **Don't implement any methods** - Abstract only
- **Don't add database-specific methods** - Keep generic (works for any backend)
- **Don't add migration methods** - Migrations come later
- **Don't add connection pooling** - Not needed for single-user app
- **Don't collapse repository and adapter transaction responsibilities back together**

### Soft Preferences (SHOULD)
- Clear docstrings explaining when to use each method
- Document typical transaction/delegation patterns in the contract guidance

---

## Implementation Notes

### What's Needed

Transaction/connection control capabilities to complete the approved contract direction:

1. **Start Transaction**
   - Purpose: Begin explicit transaction for multi-step atomic operations at the adapter layer
   - Must be followed by commit or rollback
   - Example use: Personnel status update (create new entry + mark old inactive = both must succeed)

2. **Commit Transaction**
   - Purpose: Make all changes since transaction start permanent
   - Should document potential failure modes (DatabaseError)

3. **Rollback Transaction**
   - Purpose: Discard all changes since transaction start
   - Use cases: Error recovery, test cleanup (rollback after each test)

4. **Close Connection**
   - Purpose: Clean resource shutdown
   - Use cases: Application shutdown, after test suite

5. **Repository Delegation Semantics**
   - Purpose: Clarify that repository-facing transaction methods call down into the adapter rather than reimplementing low-level transaction behavior
   - Use case: Keep higher-layer code repository-facing while preserving one low-level transaction authority

### Transaction Patterns

**Without explicit transaction**: Individual CRUD operations work independently (may auto-commit)

**With explicit transaction**: 
- Repository begins transaction
- Repository delegates to adapter transaction primitives
- Execute multiple repository operations
- Commit (success) or Rollback (failure/cleanup)

**Test isolation pattern**:
- Start transaction before test
- Execute test operations
- Rollback after test (cleanup, no permanent changes)

### Architectural Guidance

See `ai_instructions/architecture/db_architecture.md` for:
- Adapter pattern principles
- Abstract interface design
- Transaction semantics
- Repository-vs-adapter responsibility boundaries

### Affected Components

- **Modified/New**: `src/db/database_adapter.py` (transaction control methods on `DatabaseAdapter`)
- **Related contract file**: `src/db/repositories/base_repository.py` (repository-facing transaction delegation semantics)

---

## Context & Background

This is the final piece of the contract foundation. After this story:
- Low-level adapter transaction control is defined
- Repository-facing transaction delegation semantics are defined
- Connection management expectations are defined
- The contract foundation is complete enough for the SQLite adapter/repository implementation work in 001.003

**Transaction patterns**:
- **Without transaction**: CRUD methods auto-commit
- **With transaction**: begin → multiple operations → commit/rollback
- **Tests**: begin → test operations → rollback (cleanup)

---

## Testing Strategy

### Key Test Scenarios
- [ ] Transaction methods exist on `DatabaseAdapter`
- [ ] Methods are abstract (raise NotImplementedError)
- [ ] Type hints are correct
- [ ] Docstrings explain usage patterns

**Test file**: contract-focused DB tests under `tests/unit/db/`

---

## Dependencies

- **Depends on**: 
  - 001.002.001 (DatabaseAdapter base methods)
  - 001.002.002 (BaseRepository generic CRUD direction)
  - 001.002.003 (Repository specialized query contract direction)
- **Enables**:
  - 001.003 (Repository skeleton can implement all methods)
  - Test fixtures (001.008) can use transactions for isolation
  - Multi-step operations in all CRUD stories

**Note**: This completes the contract foundation direction. The SQLite adapter/repository implementation stories can now proceed on the approved architecture.

---

## Notes & Decisions

| **Date** | **Decision** | **Rationale** |
|---------|-------------|-------------|
| 2026-04-23 | Split from 001.002 | Keep stories small (<4 acceptance criteria) |
| 2026-04-23 | Separate transaction story | Cross-cutting concern, not specific to one entity |
| 2026-04-23 | Simple transaction API | begin/commit/rollback pattern familiar to developers |
| 2026-04-28 | Realigned story to adapter transaction primitives plus repository delegation | Approved architecture separates low-level DB control from repository-facing behavior |

---

**Estimated Size**: Small (~4 abstract method signatures with docstrings, ~20-40 lines depending on docstring detail)

