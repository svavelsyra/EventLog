# Epic: 003 - Communication Operational Configuration and Runtime Loading

**Epic Number**: 003  
**Created**: 2026-04-26  
**Status**: Done  
**Owner**: Developer

**Filename**: `003.000.000-operational-configuration-tables-and-runtime-loading.md`

---

## Purpose (WHY) ⭐ MOST IMPORTANT

EventLog is not meant to be a hardcoded demo. Its communication workflows, validation rules, and dynamic UI behavior depend on operational configuration that can vary by unit, mission, and deployment. Without a proper configuration layer, the application would either hardcode assumptions that quickly go stale or scatter config rules across presenters, validators, and repository code in ways that are difficult to review and change safely.

This epic exists to give the application a stable runtime source for operational metadata after the secure database foundation is in place. The purpose is to ensure that EventLog can:
- load communication ways/systems, their configured recursive option structure, and their allowed qualifiers from the database
- let validation and UI logic depend on configuration objects instead of hardcoded values
- preserve historical entry accuracy even when current configuration changes later
- support unit-specific default seed data without turning the application into a one-off custom build
- allow approved non-secret operational configuration to be exported and re-imported later when the database has been reset or re-created

The deeper reason is maintainability under real operational change. Nets move, channels get renamed, and available systems differ by unit. If configuration is not treated as a first-class capability, the rest of the application becomes brittle, inconsistent, and harder to trust.

### Success Criteria
- A clear database-backed source exists for the communication configuration needed by later workflows
- Runtime loading contracts are clear enough that presenters and validators can consume configuration without hardcoded fallback logic
- Communication configuration supports both radio and non-radio ways without silently hardcoding radio-only semantics into the shared model
- Communication system definitions, recursive configured options, current visible selection labels, and qualifier metadata can change without corrupting historical log entries
- The epic leaves enough structure for a later story to define how approved non-secret configuration can be exported and restored after `Nollställ`
- Epic 003 remains focused on configuration data and runtime loading, without absorbing structured report delivery or general settings/preferences work

---

## Goal (WHAT)

Define and deliver the configuration-data layer that sits on top of the database foundation and secure bootstrap.

This epic covers the end-to-end contract for:
1. storing operational configuration in database tables
2. seeding initial unit-specific configuration data for Phase 1 use
3. loading configuration into repository/core-facing runtime objects
4. supporting configuration-backed validation and dynamic UI population
5. establishing how configuration changes coexist with historical entry integrity
6. defining the boundary for later import/export of approved non-secret operational configuration

### Key Deliverables
- [x] Database-backed communication configuration contract built around top-level systems/ways, recursive configured options beneath them, and top-level qualifier definitions/overrides, while allowing the current GUI to stay limited to a practical first few visible levels
- [x] Seed/default-data strategy for initial operational configuration
- [x] Repository/adapter access patterns for configuration reads and targeted updates
- [x] Core/runtime configuration-loading contract, including caching/reload expectations where needed
- [x] Import/export boundary for approved non-secret operational configuration, so reset databases can be re-seeded from an external config package later
- [x] Explicit boundary between operational configuration and other concerns such as bootstrap config, structured report templates, and user preferences

---

## Limitations (CONSTRAINTS & REQUIREMENTS)

**What boundaries must we work within? What must we avoid?**

### Hard Constraints (MUST)
- **Build on the secured database path** - Epic 003 assumes configuration is loaded after the database foundation and secure startup path are available
- **Keep configuration as data, not code** - Communication systems/ways, option rows, and qualifier definitions must come from a maintainable runtime source instead of scattered hardcoded constants
- **Preserve historical log integrity** - Existing entries must remain readable and meaningful even if current configuration names, defaults, or active flags change later
- **Support the refined communication workflow model** - The communication model must support a top-level system/way plus recursive configured options beneath it, with top-level qualifiers and per-system override behavior, while the current GUI may still expose only the first few visible levels for practical use
- **Support Phase 1 seed data** - The system must be able to start with practical unit-specific defaults without requiring a full configuration-management UI first
- **Allow deliberate runtime loading** - Core validators and presenters need a defined configuration-loading path and should not each invent their own lookup logic
- **Keep the database as the source of truth** - Import/export is a portability and recovery mechanism for approved non-secret configuration, not a replacement for database-backed runtime configuration
- **Treat export/import scope as explicit and allowlisted** - A later story must define exactly which non-secret configuration is portable rather than assuming all configuration-like data should be exported
- **Keep testing with delivery stories** - Later 003.x stories should include their own tests rather than creating a broad standalone testing story

### Negative Constraints (MUST NOT)
- **Do not include secure bootstrap or encryption behavior** - That belongs to Epic 002
- **Do not absorb structured report template delivery** - Structured report forms and their template model remain Epic 004 territory for now
- **Do not absorb general user-preference storage or settings UI** - `user_preferences` and broader application settings remain outside this epic’s scope
- **Do not require a full admin/configuration screen in Phase 1** - This epic is about configuration persistence and runtime loading first, not a broad management UI
- **Do not turn this into full database backup/restore** - The planned import/export support is for approved non-secret operational configuration only
- **Do not export secrets or bootstrap/security material** - Encryption keys, key-file references, secure-bootstrap settings, passwords, and other sensitive data must remain outside this feature
- **Do not replace historical entry values with live foreign-key dependencies** - Old entries should not become incorrect or unreadable when configuration changes
- **Do not over-engineer company-level or Phase 3 multi-instance support now** - Keep the first pass aligned with the documented platoon-level Phase 1 approach while preserving room for later expansion
- **Do not absorb event metadata authority** - Event categories, event priorities, and their single runtime authority now belong to Epic `013`
- **Do not force radio-only semantics into the shared communication model** - Terms like channel and designation may still appear, but the underlying structure must stay usable for non-radio examples such as courier and other future communication ways
- **Do not enforce every real-world channel restriction in Phase 1** - Operational voice/data restrictions and similar nuances should inform the model, but Phase 1 should not become an overbuilt rule engine unless a later story explicitly chooses that scope

### Soft Constraints (SHOULD)
- Prefer small follow-up stories that separate schema/seed work, repository access, runtime config objects, and communication-specific update concerns
- Keep configuration terminology consistent across database, core, and GUI layers
- Favor explicit reload/caching behavior over implicit global state
- Support on-the-fly operational updates later without requiring a complete redesign of the persistence model

---

## Context & Background

### Architectural Context
Epic 001 established the core persistence model. Epic 002 adds protected database access and secure bootstrap. Epic 003 is the next layer for communication: the application now needs the operational metadata that tells the rest of the system what valid communication systems, configured options, and qualifiers actually exist.

Several design documents already depend on this idea:
- the communication-entry UI is configuration-driven by the selected top-level system, configured child options beneath it, and qualifier definitions/behavior
- validators are expected to receive configuration objects rather than rely on hardcoded lists
- Phase 1 intentionally uses seeded operational defaults before any broad configuration-management UI exists

### Reset and Recovery Context
`Nollställ` is intended to destroy the current database and related local artifacts quickly. That behavior is still correct, but it means database-resident operational configuration is lost along with the rest of the database state.

For that reason, Epic 003 now also needs to leave room for a later portability feature that can export and re-import **approved non-secret configuration only**. This is not meant to weaken the database-first model. It is meant to allow a clean database to be repopulated with safe operational configuration after reset, redeployment, or fresh setup.

### Structural Decision for This Epic
This epic is intentionally scoped around **operational configuration that directly supports the base communication workflow**.

That means Epic 003 focuses on:
- communication ways/systems
- recursive configured options beneath the selected top-level system/way
- the current GUI may expose only the first few visible levels for practical use, but the underlying configuration model should not be limited to those visible controls
- top-level qualifier definitions such as `encrypted` / `clear`, including per-system override behavior (selectable, forced, or hidden)
- channel/designation data where the selected system uses those concepts
- runtime config loading/caching contracts

For Phase 1 discussion, the current anchor examples are:
- `RA180` - channel-based radio setup with selectable `encrypted` / `clear`, plus practical `data` versus ordinary speech nuance
- `Motorola` - clear-only radio with channels
- `Rakel` - simplified encrypted-only radio with channels
- `Courier` - non-radio communication way included specifically to keep the shared model from becoming implicitly radio-only

It does **not** currently pull in:
- structured report templates, even though they are also configuration-shaped data
- user preferences such as `last_operator`, column layouts, or display preferences
- a full administrative UI for managing all configuration tables
- full backup/restore of operational logs or other application data
- secret/bootstrap/security material that should never be moved around as portable configuration

### Why This Is Its Own Epic
If operational configuration is deferred too long, later feature work either hardcodes placeholder values or builds ad hoc configuration logic into GUI and core layers. That would create the exact structural drift this planning phase is meant to catch early.

This epic exists to establish the shared configuration layer before feature epics depend on inconsistent assumptions.

---

## Related Epics & Dependencies

### Depends On
- **Epic 001**: Database Foundation
- **Epic 002**: Database Encryption and Secure Bootstrap

### Enables
- **Epic 006**: Communication Tab Workflow and MVP Integration, because communication-tab delivery can sit on top of a clearer configuration-loading model
- Communication feature work that needs dynamic dropdowns, validation, and seeded operational metadata
- Later configuration-management UI work, if and when it is needed

### Related Design Documents
- `ai_instructions/design/db_design.md`
- `ai_instructions/architecture/db_architecture.md`
- `ai_instructions/architecture/core_architecture.md`
- `ai_instructions/design/gui_design.md`
- `session_logs/session_005.md`

---

## User Stories

### Current Story Breakdown
- [x] [003.001 - Operational Config Schema and Phase 1 Seed Data](003.001.000-operational-config-schema-and-phase-1-seed-data.md)
- [x] [003.002 - Repository Configuration Read Models](003.002.000-repository-configuration-read-models.md)
- [x] [003.003 - Core Runtime Config Loading and Reload Contract](003.003.000-core-runtime-config-loading-and-reload-contract.md)
- [x] [003.004 - Communication Config Update Support and Quick-Add Seams](003.004.000-communication-config-update-support-and-quick-add-seams.md)
- [x] [003.006 - Non-Secret Operational Config Portability Boundary](003.006.000-non-secret-operational-config-portability-boundary.md)

**Current `003.006` Child Story Breakdown**:
- [x] [003.006.001 - Communication Portability Allowlist and Bundle Contract](003.006.001-communication-portability-allowlist-and-bundle-contract.md)
- [x] [003.006.002 - Communication Portability Export Bundle Generation](003.006.002-communication-portability-export-bundle-generation.md)
- [x] [003.006.003 - Communication Portability Import Validation and Apply Path](003.006.003-communication-portability-import-validation-and-apply-path.md)
- [x] [003.006.004 - Communication Portability Recovery Flow, Guardrails, and Round-Trip Tests](003.006.004-communication-portability-recovery-flow-guardrails-and-round-trip-tests.md)

**Moved Out During Scope Cleanup**:
- Event metadata authority moved to Epic `013`:
  - [ ] [013.001 - Event Metadata Schema and Seed Data](../ToDo/013.001.000-event-metadata-schema-and-seed-data.md)
  - [ ] [013.002 - Event Metadata Repository and Runtime Loading](../ToDo/013.002.000-event-metadata-repository-and-runtime-loading.md)
  - [ ] [013.003 - Event Category and Priority Runtime Source Cleanup](../ToDo/013.003.000-event-category-and-priority-runtime-source-cleanup.md)

**Testing Philosophy**: Each delivery story should include its own repository/core tests rather than relying on a broad standalone testing story.

---

## Notes & Decisions

| **Date** | **Decision** | **Rationale** |
|---------|-------------|-------------|
| 2026-04-26 | Created Epic 003 as the operational-configuration follow-on to Epics 001 and 002 | Keeps secure persistence separate from the configuration data that drives communication and event workflows |
| 2026-04-26 | Scoped Epic 003 around communication/event operational metadata and runtime loading | Prevents drift into unrelated settings, authentication, or broad admin UI work |
| 2026-04-26 | Kept structured report templates outside Epic 003 for now | They are tightly coupled to Epic 004 report delivery and would blur this epic’s focus |
| 2026-04-26 | Explicitly called out priority-source ambiguity as something Epic 003 must resolve | Current docs imply priority config but do not yet define a single clear runtime source |
| 2026-04-26 | Added future import/export support for approved non-secret configuration | `Nollställ` destroys the database, so later story work needs a safe way to restore portable operational configuration without treating exports as secrets or full backups |
| 2026-05-05 | Created the first focused `003.001`-`003.006` child-story set | Keeps schema/seed, repository reads, runtime loading, targeted communication updates, event metadata cleanup, and non-secret operational-config portability independently reviewable |
| 2026-05-05 | Refined Epic 003 around a neutral three-tier communication selection shape plus top-level qualifiers | Current communication-domain discussion showed that a pure system/method/channel hierarchy was too radio-shaped and too brittle for the desired flexibility |
| 2026-05-05 | Kept real-world channel restrictions as domain context rather than mandatory Phase 1 enforcement | The app needs to preserve meaningful operator choices without turning the first pass into a heavy operational rule engine |
| 2026-05-05 | Final parent-epic pass aligned the epic to the now-settled child-story direction | The underlying communication model is now described as top-level systems plus recursive configured options with bounded current UI depth, event metadata authority is expected to be singular, and portability is explicitly downstream of settled runtime sources |
| 2026-05-05 | Split non-communication event metadata out into Epic `013` | Keeps Epic `003` focused on communication configuration while still leaving event metadata tracked in a neighboring epic with its own stories |
| 2026-05-05 | Split `003.006` into smaller child stories | Portability still belongs to Epic `003`, but its contract, export, import/apply, and recovery guardrails are cleaner as separate reviewable implementation units |
| 2026-05-05 | Delivered an implementation slice for story `003.001` without treating story completion/movement as AI-owned | The user decides when a story is actually done and when it moves between `ToDo/` and `Done/` |

---

## Implementation Notes

### Current Implementation State
- Epic `003` communication-config scope now has implemented schema/seed support, repository reads and targeted updates, core runtime loading/reload behavior, and the full approved communication portability boundary.
- Focused coverage exists across `tests/unit/db/test_schema.py`, `tests/unit/db/test_communication_config_repository.py`, `tests/unit/core/test_communication_config.py`, and `tests/unit/core/test_communication_portability.py`.
- The moved-out `003.005` work no longer blocks Epic `003` completion because it now belongs to Epic `013`.
- Epic status is now marked `Done` based on the user-approved documentation closeout; the file intentionally remains in `user_stories/ToDo/` because this pass does not move files.

### Rough Story Sizes (Estimated)
- Schema and seed-data support: ~120-220 lines
- Repository/adapter configuration access: ~120-220 lines
- Runtime configuration objects/loading: ~120-220 lines
- Communication config update support: ~80-180 lines

**Total estimated**: ~500-850 lines across several small stories, depending on how much configuration-object structure is created in the core layer.

### Key Risks
- **Boundary drift** - report templates, user preferences, and admin UI can easily bloat the epic if not kept separate
- **Portability scope drift** - import/export could easily expand into full backup/restore or secret handling if the later story does not use a strict allowlist
- **Historical integrity mistakes** - over-normalizing live configuration into entry records could make old logs misleading after config changes
- **Config-loading sprawl** - presenters, validators, and repositories may each start doing their own lookups if the loading contract is weak
- **Seed-data assumptions** - unit-specific defaults must be practical without becoming undocumented one-off behavior

### Success Metrics
- Epic 003 is clear enough to split into small stories without mixing in unrelated concerns
- Later feature stories can depend on configuration objects and repository-backed metadata instead of hardcoded dropdown values
- A later story can define an explicit allowlist and flow for exporting/importing non-secret operational configuration after reset without reopening Epic 002 security concerns

---

**Next Step After Review**: Epic `003` and its current `003.x` child stories are now aligned enough for implementation-order review, starting with `003.001` if the user wants to move from planning into delivery.


